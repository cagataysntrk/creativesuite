# Belgeler biçimlendirilmez: satır tavanları (docs-size) ve elle kurulmuş tablo
# hizalamaları prettier'in yeniden akıtmasıyla bozulur. Nesir bir biçimlendiricinin işi değil.
**/*.md

# Şifreli secret'lar — biçimlendirme MAC'i bozar, dosya bir daha açılmaz.
secrets/

# Üretilmiş / türetilmiş
dist/
dist-web/
node_modules/
pnpm-lock.yaml
derived/
schemas/
# Marka token'larından ÜRETİLEN dosyalar (R-65): üreteç ile biçimlendirici aynı
# dosyaya sahip olamaz — prettier CSS'i yeniden akıtınca kapı 'güncel değil' diyor.
brand/*/derived-tokens/

# ⚠ ⚠ Üretilmiş VERİ dosyası, kaynak değil — `just gorsel-katalog` yazıyor ve insan
# okumuyor. Biçimlendirilmiş hâli 740 KB ve `repo-hygiene` kapısının 512 KB tavanını
# aşıyor; iki kapı birbiriyle çakışıyordu. Girintisiz 452 KB.
scripts/gorsel-katalog.json
