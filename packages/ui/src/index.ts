// Ring 1 — UI. Paylaşılan React bileşenleri. node:* import'u YASAK (§12).
import type { PackageIdentity } from '@suite/contracts'

export const IDENTITY: PackageIdentity = { name: '@suite/ui', ring: 'ui' }
